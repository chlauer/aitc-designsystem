Competence/topic keyword pill. Interactive only with `onClick`; selected state is solid navy.

```jsx
<Tag>Datenstrategie</Tag>
<Tag selected onClick={() => toggle("MLOps")}>MLOps</Tag>
<Tag onRemove={() => remove("Governance")}>Governance</Tag>
```