Icon-only control for toolbars, card actions, dialog close. `label` is required.

```jsx
<IconButton label="Schließen" icon={<X />} />
<IconButton label="Teilen" variant="outline" size="sm" icon={<Share2 />} />
```