Text control, 44px. Use `iconStart` for search.

```jsx
<Input placeholder="Expert:in suchen" iconStart={<Search />} />
<Input size="sm" defaultValue="2024" />
<Input invalid defaultValue="name@" />
```